# Dependencies are declared in pyproject.toml ([project.optional-dependencies]).
# Install with:
#   pip install -e ".[all]"        # all detectors
#   pip install -e ".[dev]"        # testing + linting
#   pip install -e ".[dev,all]"    # everything
#
# The scanner works with zero dependencies (stdlib only).
# Every optional dep is gated behind try/except ImportError guards.
